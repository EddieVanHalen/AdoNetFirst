<Window x:Class="AdoTest.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:v="clr-namespace:AdoTest.View"
        xmlns:vm="clr-namespace:AdoTest.ViewModel"
        mc:Ignorable="d"
        Title="MainView" Height="800" Width="800"
        ResizeMode="NoResize">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:MainPageViewModel}">
            <v:MainPageView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:MyPageViewModel}">
            <v:MyPageView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SignUpViewModel}">
            <v:SignUpView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ContentPresenter Content="{Binding CurrentViewModel, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
    </Grid>
</Window>
